[package]
name = "gather"
version = "0.1.0"
edition = "2021"
description = "Fast context gathering for AI coding agents"
license = "MIT"

[[bin]]
name = "gather-bin"
path = "src/main.rs"

[dependencies]
clap = { version = "4", features = ["derive"] }
globset = "0.4"
ignore = "0.4"
